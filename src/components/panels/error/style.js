export const style = /* CSS */ `
    .inner {
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 0.75rem;
        text-align: center;
    }

    .icon {
        font-size: 2rem;
        line-height: 1;
    }

    .inner h3 {
        font-size: 1rem;
        font-weight: 600;
        color: var(--accent);
    }

    #message {
        font-size: 0.875rem;
        color: var(--text-secondary);
        max-width: 380px;
    }
`;
